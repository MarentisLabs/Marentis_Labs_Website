# Jekyll build output
_site/
.jekyll-cache/
.jekyll-metadata

# Quarto removed — articles are now native Jekyll Markdown

# Node — Tailwind compilation (run locally, commit the compiled CSS)
node_modules/
package-lock.json

# Ruby bundler
.bundle/
vendor/
Gemfile.lock

# macOS
.DS_Store

# Editor
.vscode/
*.swp
